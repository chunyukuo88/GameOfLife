
describe("TemplateUtils.ts", () => {
  describe("utility()", () => {
    describe("WHEN: invoked,", () => {
      // eslint-disable-next-line @typescript-eslint/no-empty-function
      it("THEN: ", () => {});
    });
  });
});
