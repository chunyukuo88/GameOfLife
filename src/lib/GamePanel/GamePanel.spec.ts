import GamePanel from "./GamePanel.svelte";
import { render, screen } from "@testing-library/svelte";
import "@testing-library/jest-dom";

describe("GamePanel.svelte", () => {
  describe("Layout", () => {
    describe("WHEN: ", () => {
      it("THEN: ", () => {
        render(GamePanel);

        //
      });
    });
  });
  describe("Interaction", () => {
    describe("WHEN: ", () => {
      it("THEN: ", () => {
        render(GamePanel);

        //
      });
    });
  });
});
