import Template from "./Template.svelte";
import { render, screen } from "@testing-library/svelte";
import "@testing-library/jest-dom";

describe("Template.svelte", () => {
  describe("Layout", () => {
    describe("WHEN: ", () => {
      it("THEN: ", () => {
        render(Template);

        //
      });
    });
  });
  describe("Interaction", () => {
    describe("WHEN: ", () => {
      it("THEN: ", () => {
        render(Template);

        //
      });
    });
  });
});
